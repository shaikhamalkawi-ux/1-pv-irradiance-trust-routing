# SRRL/MIDC — Pre-Outcome Analysis Lock

**Date:** 2026-09-11 (Asia/Dubai)  
**Outcome status:** frozen before any 2023 trust/alert-transfer outcome is calculated.

This lock adds only reporting/robustness details to the already frozen protocol and schema amendment.

## Primary model
If all four admitted fields are continuously available, the primary external model uses pair disagreement, GHI component closure, dynamic disagreement, and stuck behavior. The engine is the disclosed-rule reconstruction defined in the schema amendment.

## Mandatory common-evidence sensitivity
Repeat the complete membership fitting, 2022 route calibration, and 2023 test using only the three redundant-pair features: pair disagreement, dynamic disagreement, and stuck behavior. This tests whether the external finding depends on the independent DHI/DNI closure channels.

## Date-cluster uncertainty
For the following 2023 quantities, calculate 2,000 nonparametric bootstrap replicates by local calendar date with seed 311 and report percentile 2.5%/97.5% intervals:
- per-sensor IT2-center alert rate;
- Primary-minus-Secondary IT2-center alert-rate difference;
- per-sensor median IT2 interval width;
- center-versus-lower alert-rate difference per sensor.
These are within-archive descriptive intervals, not confidence intervals for other solar sites.

## Operating-point transfer
Each route is calibrated to its own 2022 empirical 95th-percentile anomaly score. Report the realized 2023 alert burden and its difference from the nominal 5% calibration target. Do not call the difference temporal drift unless a separate support analysis justifies that language.

## Contribution interpretation
A main-paper result may be considered if the primary finding is directionally retained in the common-evidence sensitivity and the date-cluster analysis shows it is not driven by a small number of days. No minimum effect size is invented after outcomes are seen; the magnitude and uncertainty interval are reported directly and judged against practical interpretability.
